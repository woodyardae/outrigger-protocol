[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "outrigger-protocol"
version = "0.2.0"
description = "Deterministic containment and invariant gating for autonomous coding agents"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[project.scripts]
outrigger = "cli.outrigger:main"

[tool.hatch.build.targets.wheel]
packages = ["cli"]